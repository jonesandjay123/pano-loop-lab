# NEXT.md

Next useful step:

1. Open the homepage and confirm only `AXB` looks completed.
2. Confirm `BXC` and `CXA` visibly expose their unfilled work-canvas X regions.
3. Fill `BXC` externally, then add exactly one clean runtime image for it.

Do not add back legacy research candidates or old seam images.
